FoamFile {
    version     2.0;
    format      ascii;
    class       dictionary;
    location    "system";
    object      controlDict;
}

startFrom       startTime;
startTime       0;
stopAt          endTime;
endTime         18000;
deltaT          8;
writeControl    runTime;
writeInterval   3600;
libs            ("libfiniteVolumeUser.so");

