FoamFile {
    version     2.0;
    format      ascii;
    class       dictionary;
    location    "system";
    object      fvSchemes;
}

divSchemes {
    default         none;
    div(phi,T)      Gauss cubicUpwindCPCFit 3; // TODO: don't know what "3" means -- the number of dimensions?
}

gradSchemes {
    default         Gauss linear;
}

laplacianSchemes {
    default         Gauss linear corrected;
}

ddtSchemes {
    default         Euler;
}

